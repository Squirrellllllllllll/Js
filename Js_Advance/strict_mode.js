"use strict"

fullName = "Dang Le Phuc An"

function testFunc() {
    age = 18
}

testFunc()

// Use strict được thêm vào đầu file js hoặc đầu function để ngăn chặn các biến vi phạm scope tránh error code
// Báo lỗi khi xóa phần k dc xóa
// Báo lỗi trùng thuộc tính
